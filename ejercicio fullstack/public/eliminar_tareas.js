/* const deleteTarea = async () => {
  try {
    const btnValue = document.getElementsByClassName("btn-delete")
    await fetch(`http://localhost:3000/tasks/${item}`, {
      method: "DELETE",
    });
  } catch {}
};


const btnValue = document.getElementsByClassName("btn-delete");

const elementValue = btnValue[0].value;
console.log(elementValue);
 */
